/// Minecraft-independent camera, input, ray, and session behavior.
///
/// Feature implementations live only in this package's approved responsibility subpackages. The
/// entire package tree must never reference the scheduling layer or Minecraft adapters.
package io.github.leawind.thirdperson.internal.core.base;
