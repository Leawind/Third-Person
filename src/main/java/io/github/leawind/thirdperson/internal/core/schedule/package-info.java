/// Minecraft-independent policy runtime, session, and cross-feature coordination.
///
/// Feature implementations live only in this package's approved responsibility subpackages. The
/// entire package tree may use only the stable boundary types exposed by `core.base`.
package io.github.leawind.thirdperson.internal.core.schedule;
