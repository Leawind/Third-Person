# 透明度

## 修改透明度的实现原理

### `ModelPartCubeMixin`

`ModelPart.Cube#compile(PoseStack.Pose pose, VertexConsumer buffer, int packedLight, int packedOverlay, int color)`

## 机制

`EntityAgent#getSmoothOpacity()` 方法可以获取实时透明度。
